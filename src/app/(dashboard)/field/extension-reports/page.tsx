import FieldReportWorkspace from "@/components/operations/FieldReportWorkspace";

export default function ExtensionReportsPage() {
  return (
    <FieldReportWorkspace
      title="Extension officer reports"
      description="Structured extension intelligence correlated with counties and programmes."
      category="extension"
    />
  );
}
