/**
 * @deprecated Import from `@/components/enterprise` instead.
 * Re-export shim — implementation lives in the canonical enterprise layer.
 */
export { default } from "@/components/enterprise/EnterpriseDataGrid";
export type { GridColumn } from "@/components/enterprise/EnterpriseDataGrid";
